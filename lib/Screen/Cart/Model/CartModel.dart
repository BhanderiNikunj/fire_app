class CartModel {
  String? name, desc, discount, image, price, rate, size, key;
  int? quantity;

  CartModel({
    this.name,
    this.key,
    this.desc,
    this.discount,
    this.image,
    this.price,
    this.rate,
    this.size,
    this.quantity,
  });
}
