class CartModel {
  String? name, desc, discount, image, price, rate, size;

  CartModel({
    this.name,
    this.desc,
    this.discount,
    this.image,
    this.price,
    this.rate,
    this.size,
  });
}
