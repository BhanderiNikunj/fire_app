class ProfileModel {
  String? name, email, l_name, moile_no, address , image;

  ProfileModel({
    this.name,
    this.email,
    this.l_name,
    this.moile_no,
    this.address,
    this.image,
  });
}
