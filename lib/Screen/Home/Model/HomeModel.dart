class HomeModel {
  String? name, price, rate, discount, desc, brand, size, key;
  int? checkUpdate;

  HomeModel({
    this.name,
    this.price,
    this.rate,
    this.discount,
    this.desc,
    this.brand,
    this.size,
    this.checkUpdate,
    this.key,
  });
}
