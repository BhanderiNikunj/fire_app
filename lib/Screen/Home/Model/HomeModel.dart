class HomeModel {
  String? name, price, rate, discount, desc, brand, size;

  HomeModel({
    this.name,
    this.price,
    this.rate,
    this.discount,
    this.desc,
    this.brand,
    this.size,
  });
}
